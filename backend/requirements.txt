# Flask core
Flask==3.0.3
Flask-Cors==4.0.1

# Used for handling file uploads (PDF admission documents)
Werkzeug==3.0.3

# Environment variable management (used for future IBM watsonx.ai credentials)
python-dotenv==1.0.1

# Production WSGI server (optional, for deployment)
gunicorn==22.0.0
